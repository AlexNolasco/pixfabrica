# Copy to .env to customize local dev ports (optional).
# Used by vite.config.ts, pnpm dev:all, run-all, and doctor.
#
# API render limits: copy api/.env.example → api/.env (also loaded by dev:all).

PIXFABRICA_WEB_PORT=5173
PIXFABRICA_API_PORT=8000
