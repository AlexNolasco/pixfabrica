/** Full-composition timeline preview (WS /preview). */
export const LIVE_PREVIEW_ENABLED = true

/** Isolated clip preview while editing params (WS /preview/clip). */
export const CLIP_ISOLATED_PREVIEW_ENABLED = true

/** Target frame request rate while timeline transport is playing. */
export const PREVIEW_PLAY_FPS = 15

/** Discard blitted frames when render time lag exceeds this (seconds). */
export const PREVIEW_STALE_THRESHOLD_SEC = 0.1
